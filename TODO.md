# Состояние работ

Итерация 1. Обновляется в конце каждого шага.

## Сделано

| Шаг | Что | Чем проверено |
|-----|-----|---------------|
| 1 | Скелет монорепо | — |
| 2 | Go-ядро: обёртка sing-box, start/stop/status, CLI | `scripts/test-core.ps1` |
| 3 | Нативные библиотеки: cgo `.dll`/`.so`/`.dylib`, gomobile `.aar` | `scripts/smoke-desktop.ps1`, C-ABI дёргается снаружи Go |
| — | Переименование продукта в TailCore | всё пересобрано |
| 4 | Скелет Flutter, адаптивная навигация | `flutter analyze`, виджет-тесты |
| 5 | Мост Flutter ↔ ядро: FFI и MethodChannel | тест FFI против настоящей `.dll` |
| 6 | Экран соединения по макету, реальные счётчики трафика | собранное приложение, скриншоты |
| 7 | Экран серверов: импорт, выбор, Auto, фильтр, замер задержки | 39 тестов приложения + живая подписка |
| 7 | Десктопная раскладка экрана серверов — таблица | 40 тестов + собранное приложение |
| 8 | Доменный роутинг: rule-set `.srs`, тумблер «игры мимо VPN» | 44 теста приложения + разбор конфига ядром |
| 9 | Роутинг по процессам: шаблоны приложений, резолв по реальной ОС | 48 тестов приложения + разбор правил ядром |

## Шаг 7 — что осталось доделать

- [ ] **Живая проверка подключения.** Замер задержки проверен на настоящей
      подписке (три узла ответили: VLESS+Reality 215 мс, Hysteria2 310 мс,
      VLESS+TLS 219 мс). Само подключение через выбранный узел вживую ещё
      не проверялось — только через заглушечный конфиг.
- [x] **Десктопная раскладка экрана серверов** — плотная таблица со строкой
      в 44 px, шапкой колонок и акцентной полосой у выбранного узла.
      Колонок из макета меньше: REGION и LOAD рисовать нечем (ни подписка,
      ни sing-box не сообщают страну и загрузку узла), FAV — избранное,
      отдельная функция вне шага 7.
- [ ] **Скриншоты экрана серверов.** Таблица снята на собранном приложении
      с живыми узлами и выглядит как задумано, но снять её целиком не
      вышло: окно приложения перехватывалось другими окнами. Доснять,
      когда машина свободна.

## Найдено на живых данных, требует решения

- [ ] **XHTTP-узлы не поддерживаются.** В подписке пользователя два узла с
      транспортом `xhttp` — sing-box его не умеет. Сейчас они честно
      отвергаются при импорте с объяснением. Это тот самый случай, ради
      которого в ТЗ заявлен Xray-core: без него эти узлы работать не будут.
      Решить, когда подключаем Xray-core вторым движком.
- [ ] **DNS ходит мимо туннеля.** Конфиг использует DoH на 1.1.1.1 напрямую
      (`app/lib/core/singbox_config.dart`). Системный резолвер не годится:
      sing-box шлёт собственные UDP-запросы, и в сетях, где резолвер
      отвечает только штатному стеку ОС, все узлы выглядят недоступными.
      На шаге 10 DNS должен стать настройкой и ходить через туннель.
- [ ] **Локальный порт 2080 зашит константой** (`localProxyPort`). На машине
      разработчика он занят другим процессом. Настройкой станет на шаге 10.
- [ ] **Шапка экрана соединения уезжает за правый край.** На машине
      разработчика масштаб экрана 125%, окно по умолчанию 1280×720 — это
      примерно 1010 логических px, и правая группа (↓ ЗАГРУЗКА / ↑ ОТДАЧА)
      не помещается: она в `Row` негибкая и просто уходит за край, вместо
      того чтобы перенестись. Видно на собранном приложении. Код шага 6,
      чинить отдельно.

## Шаг 8 — что осталось проверить вживую

- [ ] **Скачивание rule-set вживую.** Что ядро понимает конфиг с remote
      rule-set — проверено разбором. Что список игровых доменов реально
      скачивается через туннель и игры после этого идут напрямую — нет:
      упирается в ту же живую проверку подключения, что и шаг 7.

## Шаг 9 — что осталось проверить вживую

- [ ] **Опознание процесса через локальный прокси.** Что ядро понимает
      правила `process_name`/`package_name` — проверено разбором. Что
      sing-box действительно находит процесс-владельца соединения, пока
      трафик идёт через локальный прокси (а не через TUN), — нет. Если
      окажется, что не находит, исключения по приложениям заработают
      только с шага 18.
- [ ] **Свои приложения в список.** Сейчас список — заготовка из
      одиннадцати известных приложений. Добавление своего процесса руками
      не сделано намеренно: сперва надо увидеть, что готовые работают.

## Дальше по плану

| Шаг | Что |
|-----|-----|
| 10 | Экран настроек: kill switch, TUN, автозапуск, DNS (без Subscription/Devices) |
| 11 | WireGuard: тег `with_wireguard`, разбор ссылки, формат endpoint |
| 12 | Протоколы, которые ядро уже умеет, а импорт нет: SSH, ShadowTLS, AnyTLS, Tor, NaiveProxy (тег `with_naive_outbound`) |
| 13 | Xray-core вторым движком: сборка в ту же библиотеку, выбор движка по узлу, свой статус и счётчики |
| 14 | XHTTP и остальные транспорты Xray (mKCP, QUIC от Xray) поверх шага 13 — импорт перестаёт их отвергать |
| 15 | AmneziaWG: в апстримном sing-box его нет, нужен форк ядра |
| 16 | Mieru, Juicity, TrustTunnel, olcRTC и прочее: разведка по каждому — своя библиотека, форк или отказ с объяснением |
| 17 | Android VpnService |
| 18 | Desktop TUN (Windows/Linux/macOS) |
| 19 | Полировка состояний: ошибки, «Подключение...», пустые списки |

Протоколы идут перед системным туннелем по решению заказчика. Цена
известна: до шага 17 трафик ходит через локальный прокси, а не через
TUN/VpnService, — новые протоколы проверяются в этом режиме, и на системном
туннеле их придётся проверять повторно.

### Что уже собрано в ядре, а что нет

Теги сборки живут в `scripts/build-tags.txt`, сейчас там `with_utls,with_quic`.

- **Работают:** SOCKS, HTTP(S), Shadowsocks, VMess, VLESS (в том числе
  Reality/XTLS), Trojan, Hysteria, Hysteria2, TUIC. Импорт ссылок есть.
- **Собраны, но импорта ссылок нет:** SSH, ShadowTLS, AnyTLS, Tor — зайдут
  только конфигом sing-box (шаг 12).
- **Не собраны:** WireGuard (нужен `with_wireguard`), NaiveProxy (нужен
  `with_naive_outbound`).
- **Нет в sing-box вообще:** XHTTP, mKCP и QUIC от Xray (нужен Xray-core),
  AmneziaWG (нужен форк), Mieru, Juicity, TrustTunnel, olcRTC.

## Окружение

Тулчейн целиком на `D:\TailCore\toolchain`, переменные прописаны в
пользовательский профиль:

- Go 1.26.6, gcc 16.2 (w64devkit)
- Flutter 3.47.0 / Dart 3.13.0
- JDK 21.0.12, Android SDK 36 + NDK 27.3
- Visual Studio Build Tools 2022 (для сборки под Windows)

Порядок сборки: сначала ядро (`scripts/build-desktop.ps1`), потом
приложение — без библиотеки ядра ни CMake, ни Gradle не соберутся.
